##To get started with the code, start the front end first by running the following command in the terminal:
#  npm run dev (# This will start the front end server and open the application in your default web browser.)

#To run the backend server, get into the VIR by doing the following command:
# cd backend
# conda activate open-webui (to get into the environment)
# start_windows.bat

#start the backend server by running the following command in the terminal: >